import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const FormScreens = () => {
  return (
    <View>
      <Text>FormScreens</Text>
    </View>
  );
};

export default FormScreens;

const styles = StyleSheet.create({});
